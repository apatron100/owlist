// import React from 'react';
// import PropTypes from 'prop-types';
// import classNames from 'classnames/bind';
// import styles from '../css/components/project-item';

// const cx = classNames.bind(styles);

// const ProjectItem = ({ text, id, destroyProject }) => {
//   const onDestroy = () => {
//     destroyProject(id);
//   };

//   return (
//     <li className={cx('project-item')} key={id}>
//       <span className={cx('project')}>{text}</span>
//       <button
//         className={cx('button', 'destroy')}
//         onClick={onDestroy}>{String.fromCharCode(215)}</button>
//     </li>
//   );
// };

// ProjectItem.propTypes = {
//   text: PropTypes.string.isRequired,
//   id: PropTypes.string.isRequired,
//   destroyProject: PropTypes.func.isRequired
// };

// export default ProjectItem;
